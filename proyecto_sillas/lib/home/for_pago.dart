import 'package:flutter/material.dart';

// ignore: camel_case_types
class for_pago extends StatelessWidget {
  const for_pago({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Formulario de pagos'),
      ),
      body: Center(
        child: Container(
          color: Colors.red,
          child: const Text("sss")
        ),
      )
    );
  }
}
